name: Docs

on:
  workflow_dispatch:
  push:
    branches:
      - "**"  # optional: run on all branches

concurrency:
  group: pages
  cancel-in-progress: false

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  release:
    name: Release
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # Setup Bun globally
      - name: Setup Bun
        uses: oven-sh/setup-bun@v2

      # Install dependencies inside pyeyesweb_docs
      - name: Install dependencies
        run: bun install
        working-directory: pyeyesweb_docs

      # Build SveltePress site inside pyeyesweb_docs
      - name: Build SveltePress site
        run: pnpm run build   # or `bun run build`
        working-directory: pyeyesweb_docs

      # Upload the built site
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: pyeyesweb_docs/dist   # SveltePress build output

  docs:
    name: Deploy
    needs: release
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
